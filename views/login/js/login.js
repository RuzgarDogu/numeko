console.log("Login Sayfası");
jQuery(document).ready(function($) {
  VANTA.CLOUDS({
    el: "#bodyvanta",
    mouseControls: true,
    touchControls: true,
    minHeight: 200.00,
    minWidth: 200.00
  })
});
